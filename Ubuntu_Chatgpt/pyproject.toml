[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ubuntu-computer-companion"
version = "0.1.0"
description = "A safe, local-first Ubuntu computer-use companion MVP"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["ubuntu_companion*"]

